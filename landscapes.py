import numpy as np
import matplotlib.pyplot as plt
from matplotlib import cm


class landscape:
    
    def __init__(self,
                 the_params: Tuple,
                 param_ranges: Tuple,
                 func_vals: Tuple):

    def landscape_statistics():
    
    def plot_contour():
        
        assert (len(the_params) >= 1 and len(the_params) <= 2), "Number of parameters to visualise cannot be zero or greater than two."
        
    def plot_3D():
        
        assert (len(the_params) >= 1 and len(the_params) <= 2), "Number of parameters to visualise cannot be zero or greater than two."
        
    def plot_slice():
        
        
class landscape_compare:

